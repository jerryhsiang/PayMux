{
  "name": "paymux-worker-template",
  "version": "0.1.0",
  "private": true,
  "description": "Deploy a paid API in one click with PayMux + Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "paymux": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
